/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['images.unsplash.com', 'i.imgur.com', 'arweave.net'],
  },
}

module.exports = nextConfig
